/**
 * Time calculate by millisecond.
 * @example
 * 1 second = 1000 milliseconds
 */
export enum MILLISECONDS {
    ONE_SECOND = 1000,
    ONE_MINUTE = ONE_SECOND * 60,
    ONE_HOUR = ONE_MINUTE * 60,
    ONE_DAY = ONE_HOUR * 24,
}
